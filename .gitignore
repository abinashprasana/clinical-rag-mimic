# Dataset and large files
*.csv
*.csv.gz
mimic-iv-note-deidentified-free-text-clinical-notes-2.2/

# Outputs, chunks, index, evaluation results (real MIMIC-IV-derived data --
# never commit this)
outputs/
static/eda_*.png
static/eval_*.png
*.pkl
*.index
*.json
!package.json
!package-lock.json
!vercel.json

# outputs_demo/ is the synthetic-data demo index (demo/build_index.py)
# -- fabricated notes, safe to commit, exempted from the blanket rules above
!outputs_demo/
!outputs_demo/*.pkl
!outputs_demo/*.index

# Intermediate build caches for regenerating outputs_demo's own System
# Overview charts (demo/build_chart_inputs.py,
# scripts/regenerate_ui_charts.py). Safe (synthetic-only) but not needed by a
# fresh clone -- the finished charts already live in static/demo_*.png.
outputs_demo/df_notes_pp.pkl
outputs_demo/chunks_temp.pkl
outputs_demo/ui_charts/

# Jupyter and Python caches
.venv/
.vercel-venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
node_modules/
.pnpm-store/
.pytest_cache/
.test-tmp/
playwright-report/
test-results/
*.ipynb

# Temporary files
extract.py
notebook_source.py

# Secrets
.env
